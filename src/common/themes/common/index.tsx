import spacing from '../common/spacing';
import general from '../common/general';
import {colors as colorsPalette} from '../common/palette';

export default {
  colorsPalette,
  spacing,
  general,
};
